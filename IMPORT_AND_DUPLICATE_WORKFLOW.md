# Historical Import / Update

- Existing duplicate model for new leads is unchanged.
- Only Super Admin can use Import / Update Leads.
- CSV rows with `lead_id` update the existing lead.
- CSV rows without `lead_id` create new leads.
- No new duplicate-check model is added.
- Existing Agent/Closer workflow remains unchanged.
