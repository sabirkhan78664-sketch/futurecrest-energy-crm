"use client";

/**
 * Agent-to-Agent assignment is disabled by CRM design.
 * Agents own the leads they submit; Admin/Super Admin assign Closers only.
 * This component is kept for compatibility with older imports.
 */
export default function UnassignButton() {
  return null;
}
