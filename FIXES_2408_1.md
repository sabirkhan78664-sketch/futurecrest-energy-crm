# 2408.1 Message Fix

The New Chat modal was blank because the page depended on a client-side `profiles` query.
The direct-message page now loads contacts, conversation history, sends messages, and marks messages read through authenticated server APIs using the existing service-role helper. This avoids client-side RLS preventing contacts/history from appearing.

New file:
- `app/api/messages/direct/route.ts`

Updated:
- `app/messages/page.tsx`
\n\n# 2408.1 Agent + Message follow-up\n- My Leads uses the same getMyLeads source as Agent Dashboard and shows only Lead ID, Customer Name, Fuel, Form and Status.\n- Agent dashboard keeps Today Leads, Today Sales, All Leads, All Sales, Pending Approval (non-clickable), Rejected, Call Backs and Approval Rate.\n- Messages now immediately append a successfully sent message, support message search/category filters, admin pin/edit/delete, and group read receipts when CHAT_MESSAGE_FEATURES_MIGRATION.sql is applied.\n